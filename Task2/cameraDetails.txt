# --- OPTION A: If your phone is streaming at 1080p (1920x1080) ---
# Center point (cx, cy) is exactly half the resolution: (960, 540)
camera_matrix = np.array([
    [1165.0, 0, 960.0], 
    [0, 1165.0, 540.0], 
    [0, 0, 1]
] 
dtype=np.float32)

# --- OPTION B: If your phone is streaming at 720p (1280x720) ---
# Using the same math: f = 1280 / (2 * tan(39.5)) = 776
# Center point (cx, cy) is half the resolution: (640, 360)
# camera_matrix = np.array([
#    [776.0, 0, 640.0], 
#    [0, 776.0, 360.0], 
#    [0, 0, 1]
# ], dtype=np.float32)

# We still assume zero distortion for a quick AR overlay
dist_coeffs = np.zeros((4,1))